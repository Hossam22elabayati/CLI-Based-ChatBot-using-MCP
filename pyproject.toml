[project]
name = "doc-chatbot"
version = "0.1.0"
description = "CLI document chatbot built with the Model Context Protocol (MCP)"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.0.0",
    "anthropic>=0.100.0",
]
